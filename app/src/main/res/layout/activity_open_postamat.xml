<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@android:color/white">

    <TextView
        android:id="@+id/stepText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Верните товар"
        android:textSize="25sp"
        android:textColor="@android:color/black"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="45dp" />

    <TextView
        android:id="@+id/instructionText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Откройте дверцу постамата, поместите товар внутрь и закройте дверцу"
        android:textSize="20sp"
        android:textAlignment="center"
        android:layout_marginHorizontal="40dp"
        app:layout_constraintTop_toBottomOf="@id/stepText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="30dp" />

    <Button
        android:id="@+id/doorNotOpenedButton"
        android:layout_width="215dp"
        android:layout_height="52dp"
        android:text="Дверца не открылась"
        android:background="@drawable/return_button_postamat_back"
        android:textColor="@android:color/black"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/instructionText"
        android:layout_marginTop="380dp" />

    <Button
        android:id="@+id/toReviewButton"
        android:layout_width="215dp"
        android:layout_height="52dp"
        android:text="Далее"
        android:background="@drawable/return_button_postamat_back"
        android:textColor="@android:color/black"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/doorNotOpenedButton"
        android:layout_marginTop="15dp" />

</androidx.constraintlayout.widget.ConstraintLayout>