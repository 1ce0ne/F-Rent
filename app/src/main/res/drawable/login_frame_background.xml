<!-- res/drawable/frame_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Белый фон -->
    <stroke android:width="1.5dp" android:color="@android:color/black" /> <!-- Черная рамка -->
    <corners android:radius="14dp" /> <!-- Скругление углов -->
</shape>