<!-- res/drawable/frame_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Белый фон -->
    <stroke android:width="1dp" android:color="@android:color/black" /> <!-- Черная рамка -->
    <corners android:radius="23dp" /> <!-- Скругление углов -->
</shape>